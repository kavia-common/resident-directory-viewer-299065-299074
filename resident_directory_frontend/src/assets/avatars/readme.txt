Local placeholder avatars for residents.
Files:
- avatar-1.svg ... avatar-5.svg: simple colored circle with initials slot (generic)
These assets are used by utils.getResidentAvatarUrl() when a resident has a matching image by id/slug.

You may replace these with real profile images later; filenames should follow "avatar-<id>.svg" or "avatar-<slug>.svg".
